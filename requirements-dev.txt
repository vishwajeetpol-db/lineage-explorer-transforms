# Dev-only dependencies — NOT bundled into the Databricks App.
# Used by CI and local `pytest` runs.
-r requirements.txt

pytest>=8.0.0,<9.0.0
pytest-xdist>=3.5.0,<4.0.0
httpx>=0.27.0,<1.0.0          # FastAPI TestClient dependency
