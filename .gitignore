frontend/node_modules/
.venv/
.venv*/
venv/
venv*/
__pycache__/
*.pyc
*.png
ss_*
screenshot_*

# Test / coverage artifacts
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.tox/

# Sensitive / local files
worspace_details_access_tokens
table_dependancy_graph
.env
.env.*
*.pem
*.key

# Local-only load + smoke tests (need a deployed app + costs money to run).
# Note: backend/tests/ is intentionally tracked so unit tests run in CI.
tests/load/
tests/smoke/

# Local-only demo/setup scripts (run by author against own workspace; not for customer envs).
setup_demo_lineage.py
create_stress_test.py
generate_stress_test.py

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Databricks local state
.databricks/
.claude/
